export interface RelictBase {
    relictID: number,
    name: string,
    tier: number
}

export interface RelictDetails {
    relictID: number;
    name: string;
    tier: number;
    
}

export interface TierInfo {
    tierLabel: string;
    imgPath: string;
  }